# Display configuration for the VNC session.
# Edit this file to change resolution and scaling,
# then restart the container for changes to take effect.

# VNC framebuffer resolution.
RESOLUTION=1920x1080

# Integer scale factor.
# 1 = standard (suitable for 1080p displays)
# 2 = HiDPI   (suitable for 5K displays)
SCALE=1
